---
// src/pages/plan-du-site.astro
import Layout from '../layouts/Layout.astro';
import { SITE } from '../config';
import { SERVICES } from '../data/services';
import { VILLES } from '../data/villes';

// Prestations → pages dédiées /services/<slug>/
const prestations = SERVICES.map(s => ({ href: `/services/${s.slug}/`, nom: s.nom }));
// Pages villes → /entreprise-nettoyage-<slug>/
const villes = VILLES.map(v => ({ href: `/entreprise-nettoyage-${v.slug}/`, nom: v.nom }));
---

<Layout
  title="Plan du site — NPG"
  description="Plan du site de NPG, entreprise de nettoyage dans l'Essonne (91)."
  canonical={`${SITE.url}/plan-du-site`}
>

  <section class="page-hero">
    <div class="container">
      <nav class="breadcrumb">
        <a href="/">Accueil</a><span>›</span><span>Plan du site</span>
      </nav>
      <h1>Plan du site</h1>
    </div>
  </section>

  <div class="content-wrap">
    <div class="container sitemap">

      <div class="sitemap__section">
        <h2>Pages principales</h2>
        <ul class="sitemap__list">
          <li><a href="/">Accueil</a></li>
          <li><a href="/a-propos/">À propos</a></li>
          <li><a href="/contact/">Contact</a></li>
          <li><a href="/galerie/">Galerie</a></li>
          <li><a href="/mentions-legales/">Mentions légales</a></li>
          <li><a href="/politique-de-cookies-ue/">Politique de cookies</a></li>
        </ul>
      </div>

      <div class="sitemap__section">
        <h2>Nos prestations</h2>
        <p style="margin:0 0 .75rem;"><a href="/services/"><strong>Toutes nos prestations de nettoyage</strong></a></p>
        <ul class="sitemap__list">
          {prestations.map(p => (
            <li><a href={p.href}>{p.nom}</a></li>
          ))}
        </ul>
      </div>

      <div class="sitemap__section">
        <h2>Zones d'intervention</h2>
        <p style="margin:0 0 .75rem;">Nettoyage dans l'Essonne (91), commune par commune :</p>
        <ul class="sitemap__list">
          {villes.map(v => (
            <li><a href={v.href}>Nettoyage à {v.nom}</a></li>
          ))}
        </ul>
      </div>

    </div>
  </div>

</Layout>
